{
  "name": "frontend-skeletons",
  "version": "1.0.0",
  "description": "Premium frontend foundations library — 100+ website skeletons",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "node scripts/generate-all.mjs",
    "screenshot": "node scripts/screenshot-all.mjs",
    "catalog": "node scripts/generate-catalog.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "playwright": "^1.50.0"
  }
}
